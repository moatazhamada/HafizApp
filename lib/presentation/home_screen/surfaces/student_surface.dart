import 'package:flutter/material.dart';

/// Placeholder for the Student surface.
/// Shows dashboard cards with progress, goals, and practice lists.
class StudentSurface extends StatelessWidget {
  const StudentSurface({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Student Surface — Coming in Phase 3'),
    );
  }
}
