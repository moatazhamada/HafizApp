import 'package:flutter/material.dart';

/// Placeholder for the Seeker surface.
/// Shows search-first layout with discovery cards.
class SeekerSurface extends StatelessWidget {
  const SeekerSurface({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Seeker Surface — Coming in Phase 3'),
    );
  }
}
