<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@drawable/widget_background"
    android:padding="16dp"
    android:gravity="center">

    <TextView
        android:id="@+id/widget_verse_arabic"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:textSize="18sp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:textDirection="rtl"
        android:textColor="#1A1A1A"
        android:maxLines="4"
        android:ellipsize="end"
        android:text="بِسْمِ اللَّهِ الرَّحْمَـٰنِ الرَّحِيمِ" />

    <TextView
        android:id="@+id/widget_verse_english"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:textColor="#666666"
        android:maxLines="2"
        android:ellipsize="end"
        android:layout_marginTop="8dp"
        android:text="In the name of Allah, the Entirely Merciful, the Especially Merciful." />

    <TextView
        android:id="@+id/widget_verse_ref"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:fontFamily="sans-serif"
        android:gravity="center"
        android:textColor="#999999"
        android:layout_marginTop="4dp"
        android:text="— 1:1" />

</LinearLayout>
