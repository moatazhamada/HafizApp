<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Modern Material3 theme for the launch window (light) -->
    <style name="LaunchTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Splash background shown before first Flutter frame -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowSplashScreenBackground">@color/splash_background</item>
        <!-- Edge-to-edge: draw behind system bars -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>

    <!-- Material3 app theme used once Flutter initializes -->
    <style name="NormalTheme" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">?android:colorBackground</item>
        <!-- Edge-to-edge: draw behind system bars -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
    </style>
</resources>
